# Model assets

Export the trained XGBoost assets from Colab and place them in this directory:

- `xgboost_model.json`
- `model_metadata.json`

The Vercel API will report `configuration_required` until both files exist.
Do not place training datasets or patient CSV files in this repository.
